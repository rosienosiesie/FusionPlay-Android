# 参与贡献

感谢为 FusionPlay 提交改进。

## 开发流程

1. 从主分支创建范围明确的功能或修复分支。
2. 不提交 APK、构建缓存、设备身份状态、日志、签名文件或本机配置。
3. 保持 Flutter、Kotlin 和 Rust 之间的协议边界清晰，避免在界面层复制后端状态机。
4. 行为变化应补充最接近该逻辑层的自动化测试。
5. 提交前运行 `flutter analyze`、`flutter test`、Kotlin 单元测试和相关 Rust 测试。

## 兼容性要求

- 最低 Android API 21。
- 发布 APK 同时支持 `armeabi-v7a`、`arm64-v8a` 和 `x86_64`。
- 不依赖厂商私有运行库、账号服务、硬编码设备凭据或不可再分发组件。
- 协议兼容修改应记录可公开复现的依据，避免提交真实用户抓包中的个人信息。

## 许可证

提交贡献即表示你有权提交相关内容，并同意贡献内容按仓库的 GNU AGPL v3.0 许可证发布。第三方来源的代码或资源必须保留原许可证和归属说明。
